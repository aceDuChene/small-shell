To compile, type into the linux terminal
gcc -g -Wall -std=gnu99 main.c -o smallsh

then to run, type in the terminal
./smallsh

I am aware of at least one error that shows up in wall,
"function returns address of local variable" so hopefully
I can fix that before the due date. Thanks!